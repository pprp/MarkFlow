# MF-100 - Strict mode enforces GFM-style heading whitespace and list indentation rules

## Notes
Implemented on 2026-04-16 in `packages/editor/src/markdownMode.ts`, `packages/editor/src/App.tsx`, `packages/editor/src/editor/MarkFlowEditor.tsx`, `packages/editor/src/editor/outline.ts`, `packages/editor/src/editor/indexer.ts`, and `packages/editor/src/editor/decorations/tocDecoration.ts` by adding a persisted local strict/tolerant markdown-mode preference, exposing it from the status bar, and threading the selected parser mode through the editor, outline, and symbol-index paths. Automated coverage in `packages/editor/src/editor/__tests__/markdownMode.test.ts`, `packages/editor/src/editor/__tests__/MarkFlowEditor.test.tsx`, `packages/editor/src/editor/__tests__/outline.test.ts`, `packages/editor/src/editor/__tests__/indexer.test.ts`, and `packages/editor/src/__tests__/App.test.tsx` passed on 2026-04-16, alongside `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. A small prerequisite fix in `packages/editor/src/components/GlobalSearch.tsx` / `.css` replaced blur-driven dismissal with backdrop dismissal so the existing navigation-history tests stay stable while the editor reclaims focus. `passes` remains false until manual verification confirms the persisted toggle and restart behavior against the same heading/list fixture. Sources: https://support.typora.io/Strict-Mode/ and https://support.typora.io/Markdown-Reference/.

## Steps
- Enable strict mode from a settings surface, restart if required, and type `###Header`; confirm it is not treated as a heading until a whitespace is added after the `#` characters.
- Create ordered-list paragraphs and sublists with insufficient indentation and confirm strict mode follows GFM-style indentation instead of Typora's tolerant behavior.
- Disable strict mode and confirm the same fixture returns to the more forgiving parsing behavior.

## Automated Verification
- Add parser/editor tests that run the same heading and list-indent fixtures under strict and tolerant modes.
- Add settings-persistence coverage if the toggle is stored outside the editor state.

## Manual Verification
- Toggle strict mode on the same markdown fixture, restart, and verify heading/list parsing changes exactly as expected.
