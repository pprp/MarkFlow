# MF-084 - Fullscreen and distraction-free modes hide window chrome and side panels

## Notes
Implemented on 2026-04-16 in `packages/desktop/src/main/index.ts`, `packages/desktop/src/main/menu.ts`, `packages/desktop/src/main/menu.test.ts`, `packages/desktop/src/preload/index.ts`, `packages/shared/src/index.ts`, `packages/editor/src/App.tsx`, `packages/editor/src/styles/global.css`, and `packages/editor/src/__tests__/App.test.tsx`. Fullscreen now toggles Electron native fullscreen with `F11` on Windows/Linux and `Ctrl+Command+F` on macOS, the main process publishes true window fullscreen state into the renderer, and both fullscreen plus distraction-free mode hide titlebar/tabstrip/statusbar/sidebar/outline/minimap while preserving the pre-mode panel state for exact restoration on exit. Automated verification passed on 2026-04-16 with `pnpm --filter @markflow/desktop exec vitest run src/main/menu.test.ts`, `pnpm --filter @markflow/editor exec vitest run src/__tests__/App.test.tsx`, `pnpm --filter @markflow/shared lint`, `pnpm --filter @markflow/shared build`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, `pnpm --filter @markflow/desktop lint`, `pnpm --filter @markflow/desktop build`, `./harness/init.sh --smoke`, and `pnpm harness:verify`. `passes` remains false because the required long-document manual flicker check was not completed in this terminal session. Sources: https://support.typora.io/Menu/ and https://support.typora.io/Shortcut-Keys/.

## Steps
- Trigger fullscreen (F11 / Cmd+Ctrl+F) and confirm the OS window chrome is hidden.
- Trigger distraction-free mode and confirm sidebars/outline/toolbars are hidden while the document stays centered.
- Exit both modes and confirm prior panel visibility is restored exactly.

## Automated Verification
- Add tests asserting panel visibility state is saved before entering each mode and restored after exit.

## Manual Verification
- Alternate between fullscreen, distraction-free, and default view on a long document and confirm no flicker.
