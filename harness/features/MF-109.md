# MF-109 - App shell module decomposition shrinks App.tsx into focused feature hooks and components

## Notes
Implemented on 2026-04-17 in `packages/editor/src/App.tsx`, `packages/editor/src/app-shell/documents.ts`, `packages/editor/src/app-shell/useSearchDialogs.ts`, `packages/editor/src/app-shell/useStatusBarPanels.ts`, `packages/editor/src/app-shell/AppStatusBar.tsx`, `packages/editor/src/app-shell/useCommandPaletteActions.ts`, `packages/editor/src/app-shell/useDesktopBridge.ts`, and `packages/editor/src/app-shell/useNavigationHistoryController.ts`. The app shell now splits desktop IPC/startup hydration, status-bar statistics UI, search dialog state, command-palette registration, shared document helpers, and navigation-history control into focused modules, shrinking `App.tsx` from 3,896 lines at `HEAD` to 1,902 lines without changing the public UI contract. Automated verification passed on 2026-04-17 with `pnpm --filter @markflow/editor exec vitest run src/__tests__/App.test.tsx`, `pnpm --filter @markflow/editor exec vitest run`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. Incremental rebuild time stayed effectively flat in a like-for-like Vite watch comparison against a detached `HEAD` worktree: baseline rebuilds were `3808 ms`, `4097 ms`, `4218 ms` (avg `4041 ms`) versus current rebuilds `3981 ms`, `3943 ms`, `4077 ms` (avg `4000 ms`). `passes` remains false because the remaining 180k keystroke-latency proof could not be completed truthfully from this terminal session: a CDP-driven Electron dev harness could stand up Vite + Electron and attach to the renderer, but the large-fixture startup hydration path did not yield a trustworthy mid-document typing probe, so no live latency number is recorded.

## Steps
- Split `packages/editor/src/App.tsx` (currently ~3,910 LOC) into focused hooks/components: desktop IPC bridge, statistics surface, search dialogs, vault/session hydration, palette registration, and navigation history.
- Confirm behavior parity with the existing integration tests in `packages/editor/src/__tests__/App.test.tsx` (all 52 cases still pass).
- Verify incremental rebuild time and keystroke latency on the 180k fixture are not worse than before the refactor.

## Automated Verification
- pnpm --filter @markflow/editor exec vitest run
- pnpm --filter @markflow/editor lint
- pnpm --filter @markflow/editor build

## Manual Verification
