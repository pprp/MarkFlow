# MF-067 - Alternate LaTeX delimiters `\(...\)` and `\[...\]` render as math in WYSIWYG mode

## Notes
Implemented on 2026-04-15 in `packages/editor/src/editor/decorations/mathDecoration.ts` by extending the existing KaTeX decoration path to recognize inline `\(...\)`, single-line `\[...\]`, and multi-line `\[` / `\]` display blocks while preserving caret reveal and code-range exclusion. `packages/editor/src/editor/__tests__/mathDecoration.test.ts` now covers alternate-delimiter rendering, caret-inside source reveal, multi-line block handling, and fenced/inline-code exclusion. Automated verification passed on 2026-04-15 with the listed math test command plus `pnpm --filter @markflow/editor lint` and `pnpm --filter @markflow/editor build`. `passes` remains false because the manual desktop mixed-delimiter verification is still pending. Sources: https://support.typora.io/What%27s-New-1.11/ and https://support.typora.io/Math/.

## Update 2026-04-19
- Re-ran the required automated verification on the current tree:
  - `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/mathDecoration.test.ts`
  - `pnpm --filter @markflow/editor lint`
  - `pnpm --filter @markflow/editor build`
  - `pnpm harness:verify`
- Manual verification completed on 2026-04-19 in the current production renderer preview at `http://localhost:4173`:
  - replaced the starter document with a mixed-delimiter fixture containing `$...$`, `$$...$$`, `\(...\)`, and `\[...\]`,
  - confirmed Preview mode stayed active and the live DOM rendered exactly `2` `.mf-math-inline` widgets plus `2` `.mf-math-block` widgets,
  - moved the caret into `\(a^2+b^2=c^2\)` and observed the inline widget count drop from `2` to `1` while raw `\(a^2+b^2=c^2\)` source reappeared,
  - moved the caret into `\[\int_0^1 x^2 dx\]` and observed the block widget count drop from `2` to `1` while raw `\[\int_0^1 x^2 dx\]` source reappeared,
  - confirmed `view.state.doc.toString()` stayed byte-for-byte identical to the fixture after both caret moves, so delimiter source remained editable without corruption.
- With that live proof plus the automated verification above, `harness/feature-ledger.json` can move to `status=verified`, `passes=true`, and `lastVerifiedAt=2026-04-19T10:35:59Z`.

## Steps
- Open markdown containing inline `\(a^2+b^2=c^2\)` and block `\[\int_0^1 x^2 dx\]` math delimiters.
- Move the caret out of each expression and confirm it renders through the existing KaTeX path.
- Re-enter the expression and confirm raw source delimiters stay editable without corruption.

## Automated Verification
- pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/mathDecoration.test.ts
- pnpm --filter @markflow/editor lint
- pnpm --filter @markflow/editor build

## Manual Verification
- In desktop editor, verify mixed `$...$`, `$$...$$`, `\(...\)`, and `\[...\]` expressions in one document render consistently.
