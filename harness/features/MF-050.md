# MF-050 - Background indexer builds a symbol table for headings and anchors without blocking the UI thread

## Notes
Implemented on 2026-04-15 in `packages/editor/src/editor/indexer.ts`, `packages/editor/src/editor/MarkFlowEditor.tsx`, `packages/editor/src/App.tsx`, and `packages/editor/src/editor/outline.ts`. The background indexer now feeds the live outline and internal-anchor navigation through `symbolTableField` plus `onSymbolTableChange`, performs an immediate off-stack initial scan, keeps subsequent rescans debounced, and drops stale async results when documents switch mid-index. Automated coverage in `src/editor/__tests__/indexer.test.ts`, `src/editor/__tests__/outline.test.ts`, `src/editor/__tests__/MarkFlowEditor.test.tsx`, and `src/__tests__/App.test.tsx` passed on 2026-04-15 with the listed editor test command, plus `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. `passes` remains false because the manual 180k-line typing/no-lag verification is still pending.

## Steps
- Open a 180k-line document.
- Confirm the outline panel populates asynchronously within 5 seconds without freezing typing.
- Confirm anchor links in the document resolve correctly against the symbol table.

## Automated Verification
- pnpm --filter @markflow/editor test:run -- src/editor/__tests__/indexer.test.ts src/editor/__tests__/outline.test.ts src/editor/__tests__/MarkFlowEditor.test.tsx src/__tests__/App.test.tsx

## Manual Verification
- Type continuously while the document loads; confirm no input lag.
