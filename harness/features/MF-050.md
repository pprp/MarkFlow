# MF-050 - Background indexer builds a symbol table for headings and anchors without blocking the UI thread

## Notes
Implemented on 2026-04-15 in `packages/editor/src/editor/indexer.ts`, `packages/editor/src/editor/MarkFlowEditor.tsx`, `packages/editor/src/App.tsx`, and `packages/editor/src/editor/outline.ts`. The background indexer now feeds the live outline and internal-anchor navigation through `symbolTableField` plus `onSymbolTableChange`, performs an immediate off-stack initial scan, keeps subsequent rescans debounced, and drops stale async results when documents switch mid-index. On 2026-04-17 the batching path was first tightened so it scans line batches incrementally instead of rebuilding ever-larger prefix strings, preserves a running scan until the newer debounced restart actually begins, and stops serializing the entire document just to publish symbol-table changes. Later the same day, the remaining 180k-line hot path was reduced again: `indexer.ts` now streams batch lines without a full upfront `split('\n')` and uses a larger `2,000`-line batch size so early outline headings arrive sooner, `MarkFlowEditor.tsx` suppresses parent-echo `onChange` callbacks when external content replacements are already in sync, `App.tsx` skips no-op content/selection writes and stops recomputing full-document total lines for ordinary tabs, and `AppStatusBar.tsx` moves document statistics onto deferred content so render-path full-text scans do not compete with typing. In a further 2026-04-17 follow-up, `App.tsx` now debounces renderer-side recovery-checkpoint IPC by `750ms` so dirty 180k-line tabs no longer serialize their full content across Electron on every keystroke, and `src/__tests__/App.test.tsx` now locks both delayed checkpoint scheduling and rapid-edit coalescing. Another 2026-04-17 pass trimmed the active editor sync path: `packages/editor/src/largeDocument.ts` now centralizes the large-doc threshold, `MarkFlowEditor.tsx` defers full `onChange` materialization by `200ms` for documents above `1,000,000` chars while still exposing synchronous `getContent()` reads, and `App.tsx` now sources save/export/recovery/snapshot flows from the live active editor buffer instead of waiting for tab-state content sync. The next 2026-04-17 pass removed two more remaining full-document reparses from the large-file path: `indexer.ts` now accepts CodeMirror `Text` snapshots directly so background indexing no longer starts with `doc.toString()` on every edit, `tocDecoration.ts` reuses `symbolTableField` for large documents instead of reparsing headings again, and `markdownPostProcessor.ts` now caches `sourceText` across selection/viewport-only updates. The latest 2026-04-17 pass reduced one more large-document React pressure point: `packages/editor/src/largeDocument.ts` now defines a shared `120ms` symbol-table publication cadence, `MarkFlowEditor.tsx` coalesces large-document `onSymbolTableChange` callbacks before they reach the App shell, and `src/editor/__tests__/MarkFlowEditor.test.tsx` now locks that coalescing behavior while preserving the final symbol table. A further 2026-04-17 follow-up removed another pair of App-shell typing hot paths: `MarkFlowEditor.tsx` now reports the current cursor line number directly from CodeMirror, `App.tsx` uses that line number instead of rescanning the whole document on cursor-driven shell updates, symbol-table/collapsed-range/cursor/viewport observers now run in `startTransition(...)`, and no-op `updateTab(...)` calls now bail out before `setTabs(...)` triggers an unnecessary full-shell render. Regression coverage in `src/editor/__tests__/indexer.test.ts`, `src/editor/__tests__/outline.test.ts`, `src/editor/__tests__/MarkFlowEditor.test.tsx`, `src/editor/__tests__/tocDecoration.test.tsx`, `src/editor/__tests__/markdownPostProcessor.test.tsx`, and `src/__tests__/App.test.tsx` passed again on 2026-04-17 with the listed editor test command (now 40 files / 450 tests with 3 skipped), plus `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. A further 2026-04-17 verification pass escalated from the previously blocked dev-renderer CDP probe to the current-source packaged desktop app: `pnpm desktop:pack` rebuilt the app from the current tree, the zip was unpacked to `/tmp/markflow-current-app/MarkFlow.app`, and that app opened `/tmp/mf050-live-180k.md` (a temporary copy of `harness/fixtures/mf-large-180k.md` with `[Jump to Section 1](#section-1)` inserted near the top). That live app now goes further than the earlier notes claimed: once the packaged renderer settles, page-level CDP can read DOM state again, the live outline reaches 60 items, and a settled ctrl-click on the injected internal link moves the active outline item from the fixture title to `Section 1`. The remaining blocker is now narrower and more specific: on a fresh launch the editor first became script-readable with 11 outline items at about `4.24s`, but the first post-focus typing-stage `Runtime.evaluate('performance.now()')` still timed out after `3s` while the outline was only partially populated. That means this session still could not truthfully satisfy the required “type continuously while the document loads; confirm no input lag” gate. `passes` therefore still remains false and `lastVerifiedAt` stays null until someone can complete or instrument that final fresh-launch typing proof without the renderer stalling mid-probe.

## Steps
- Open a 180k-line document.
- Confirm the outline panel populates asynchronously within 5 seconds without freezing typing.
- Confirm anchor links in the document resolve correctly against the symbol table.

## Automated Verification
- pnpm --filter @markflow/editor test:run -- src/editor/__tests__/indexer.test.ts src/editor/__tests__/outline.test.ts src/editor/__tests__/MarkFlowEditor.test.tsx src/__tests__/App.test.tsx

## Manual Verification
- Type continuously while the document loads; confirm no input lag.
