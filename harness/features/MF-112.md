# MF-112 - Performance budget guards editor open-to-interactive and keystroke latency on large documents

## Notes
No current perf guardrail exists despite the 180k-line fixture. A simple baseline + 10% threshold prevents silent slowdowns as features stack.

## Steps
- Capture open-to-interactive time, average keystroke latency, and scroll frame time on `harness/fixtures/mf-large-180k.md` using a Playwright or Vitest-browser harness.
- Store the baseline in the repo and fail CI when any metric regresses by more than 10%.
- Expose the latest metrics as a summary comment on pull requests.

## Automated Verification
- pnpm --filter @markflow/editor exec vitest run
- pnpm harness:verify

## Manual Verification
