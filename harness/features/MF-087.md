# MF-087 - Flowchart, sequence, and Mermaid 11 diagram fences render live previews for Gantt, packet, kanban, architecture, radar, treemap, Venn, and Ishikawa charts

## Notes
Typora supports `flow`, `sequence`, and Mermaid diagram fences, including Gantt charts, via its documented diagram workflow. Sources: https://support.typora.io/Draw-Diagrams-With-Markdown/ and https://support.typora.io/What%27s-New-1.0/.

Verified-only closure on 2026-04-25T20:07:39+0800: no product code changes were required in this run. MarkFlow already routes `flow`, `sequence`, and `mermaid` fenced blocks through the shared diagram decoration pipeline, normalizes bare `flow` and `sequence` fences into Mermaid-compatible sources, and passes raw `mermaid` blocks through unchanged. Coverage is split across two test cases (to fit within the JSDOM render viewport): one covering `flow`, `sequence`, `sequenceDiagram`, `gantt`, `venn-beta`, and `ishikawa`; a second covering `packet-beta`, `kanban`, `architecture-beta`, `radar-beta`, and `treemap-beta`. Together they prove all 11 Mermaid chart types render through the shared pipeline, that one broken diagram stays block-local, and that edited diagrams re-render without duplicate DOM nodes. This run did not compare live output against Typora side by side; confidence depends on the shared Mermaid engine plus the targeted diagram-decoration coverage below.

## Steps
- Author fenced blocks with languages `flow`, `sequence`, and `mermaid`, including Mermaid 11 `gantt`, `packet-beta`, `kanban`, `architecture-beta`, `radar-beta`, `treemap-beta`, `venn-beta`, and `ishikawa` sources, and confirm each renders an inline diagram preview.
- Introduce a syntax error in one block and confirm the editor surfaces a localized render error without breaking siblings.
- Edit the source and confirm the diagram re-renders on next idle without duplicate DOM nodes.

## Automated Verification
- `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/mermaidDecoration.test.ts`
- Existing coverage in [`/Users/pprp/Workspace/MarkFlow/packages/editor/src/editor/__tests__/mermaidDecoration.test.ts`](/Users/pprp/Workspace/MarkFlow/packages/editor/src/editor/__tests__/mermaidDecoration.test.ts) proves:
  - `flow` fences normalize into Mermaid input
  - `sequence` fences normalize into Mermaid input
  - raw `mermaid` fences pass `sequenceDiagram`, `gantt`, `packet-beta`, `kanban`, `architecture-beta`, `radar-beta`, `treemap-beta`, `venn-beta`, and `ishikawa` sources through unchanged
  - one broken diagram shows a block-local error while siblings still render
  - edited diagrams re-render without duplicate DOM nodes
- `pnpm harness:verify`

## Manual Verification
- No remaining manual-only blocker. If desired, open the Typora diagram sample document and compare one `flow`, one `sequence`, and one Mermaid `gantt` block against MarkFlow as an optional parity smoke check.
