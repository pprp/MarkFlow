# MF-087 - Flowchart, sequence, and gantt fences render alongside Mermaid

## Notes
Typora supports flowchart.js, js-sequence-diagrams, and Mermaid in fenced blocks. Sources: https://support.typora.io/Draw-Diagrams-With-Markdown/ and https://support.typora.io/What%27s-New-1.0/.

## Steps
- Author fenced blocks with languages `flow`, `sequence`, `mermaid` (sequence/gantt), and confirm each renders an inline diagram preview.
- Introduce a syntax error in one block and confirm the editor surfaces a localized render error without breaking siblings.
- Edit the source and confirm the diagram re-renders on next idle without duplicate DOM nodes.

## Automated Verification
- Expand `mermaidDecoration.test.ts` (or add `diagramDecoration.test.ts`) to cover the additional language tags and error isolation.

## Manual Verification
- Open the Typora diagram sample document and confirm parity rendering.
