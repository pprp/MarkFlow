# MF-076 - Paste as plain text shortcut strips rich formatting before insertion

## Notes
Implemented on 2026-04-15 in `packages/editor/src/editor/extensions/smartPaste.ts` by tracking a short-lived Cmd/Ctrl+Shift+V intent window so the next paste uses `text/plain` even when the clipboard also carries HTML, while preserving the default HTML→Markdown smart-paste path and existing image-paste priority. Focused coverage in `packages/editor/src/editor/__tests__/smartPaste.test.ts` now verifies default rich-text conversion, both `Ctrl+Shift+V` and `Cmd+Shift+V` plain-text shortcut paths, one-shot shortcut reset, blur reset, 1-second timeout expiry, and image-paste precedence. Sources: https://support.typora.io/Copy-Paste/ and https://support.typora.io/Shortcut-Keys/.

Updated on 2026-04-19: re-ran the session-start protocol (`pnpm harness:start`, `./harness/init.sh --smoke`) plus the required automated verification (`pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`) on the current tree, and all of those commands passed again after expanding `smartPaste.test.ts` with the missing macOS shortcut and reset-window coverage. I then built the current-source packaged desktop app with `pnpm desktop:pack`, unpacked it to `/tmp/mf076-markflow-app/MarkFlow.app`, and pushed the manual verification further with a live browser clipboard probe: a real `Microsoft Edge` page copy exposed `«class HTML»` plus plain-text clipboard flavors for `Bold link and code`, and a native `Cmd+V` into the packaged MarkFlow app saved `**Bold** [link](https://example.com/) and \`code\`` to disk, confirming the default webpage paste path still takes the HTML-to-markdown conversion route. The ledger still cannot move to `status=verified` or `passes=true` in this environment, because the full acceptance matrix remains incomplete: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returns no `Microsoft Word.app` bundle, the native `Cmd+Shift+V` desktop automation path did not produce a trustworthy paste event for the webpage case, and the `Code` source probe did not yield stable clipboard evidence. I did not substitute Pages/TextEdit for Word or treat the split automation probes as manual proof, so `harness/feature-ledger.json` remains truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Follow-up on 2026-04-19: re-ran the required automated verification on the current tree (`pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`) and rebuilt the current-source packaged desktop app with `pnpm desktop:pack`, unpacked to `/tmp/mf076-packaged/MarkFlow.app`. Live probes against the packaged app narrowed the blocker further. A real `Microsoft Edge` copy from `/tmp/mf076-web-source.html` reached MarkFlow with `text/plain` and `text/html` clipboard types, and native `Cmd+V` inserted `**Bold** [link](https://example.com) and \`code\`` in Source mode, confirming the default webpage conversion path end-to-end. A real `Visual Studio Code` copy from `/tmp/mf076-code-source.md` reached MarkFlow with `text/plain`, `text/html`, `application/vnd.code.copymetadata`, and `vscode-editor-data`, and native `Cmd+V` inserted `**Bold** [link](https://example.com) and \`code\`` unchanged in Source mode. The ledger still cannot move to `status=verified` or `passes=true` in this environment, because the native `Cmd+Shift+V` automation path into the packaged app still did not fire any paste event for the webpage case (`window.__mf076LastPaste` stayed `null` and `/tmp/mf076-web-plain.md` stayed empty), and `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returns no `Microsoft Word.app` bundle. I kept `harness/feature-ledger.json` truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Follow-up on 2026-04-20: re-ran the session-start protocol (`pnpm harness:start`, `./harness/init.sh --smoke`) and the required automated verification (`pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`) on the current tree, and all of those commands passed again. I did not promote `harness/feature-ledger.json`, because the manual acceptance matrix is still blocked by the environment: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` again returned no `Microsoft Word.app` bundle, and an `/Applications` probe only exposed `Microsoft Edge.app`, `Safari.app`, and `Visual Studio Code.app` among the required external sources. Given that missing Word alone keeps the matrix incomplete, I did not reuse the earlier partial webpage/VS Code probes to overstate completion. `status` remains `ready`, `passes` remains `false`, and `lastVerifiedAt` remains `null` until the Word/webpage/VS Code with-and-without-shortcut checklist is completed in a trusted desktop session.

## Steps
- Copy rich-text content from an external source.
- Press Cmd/Ctrl+Shift+V in the editor and confirm the inserted text contains no markdown conversion and no HTML residue.
- Repeat with Cmd/Ctrl+V and confirm smart-paste conversion (MF-026) still applies on the default paste path.

## Automated Verification
- pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts
- pnpm --filter @markflow/editor lint
- pnpm --filter @markflow/editor build

## Manual Verification
- Paste from three rich sources (Word, webpage, VS Code) with and without the shortcut and compare results.
- 2026-04-19 partial live evidence:
  - A real `Microsoft Edge` page copy from `/tmp/mf076-web-source.html` exposed `text/plain` plus `text/html` clipboard flavors, and native `Cmd+V` into `/tmp/mf076-packaged/MarkFlow.app` inserted `**Bold** [link](https://example.com) and \`code\`` in MarkFlow Source mode.
  - A real `Visual Studio Code` copy from `/tmp/mf076-code-source.md` exposed `text/plain`, `text/html`, `application/vnd.code.copymetadata`, and `vscode-editor-data`, and native `Cmd+V` into `/tmp/mf076-packaged/MarkFlow.app` inserted `**Bold** [link](https://example.com) and \`code\`` in MarkFlow Source mode.
  - The native `Cmd+Shift+V` automation path still did not produce any paste event for the packaged-app webpage case (`window.__mf076LastPaste` stayed `null`, and `/tmp/mf076-web-plain.md` stayed empty), so the plain-text shortcut step remains unverified.
  - `Microsoft Word.app` is still not installed in this environment (`mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` returned no results).
  - 2026-04-20 environment re-check: `/Applications` still only exposed `Microsoft Edge.app`, `Safari.app`, and `Visual Studio Code.app` among the required external sources, so the full three-source matrix remains blocked until `Microsoft Word.app` is available.
