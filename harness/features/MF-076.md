# MF-076 - Paste as plain text shortcut strips rich formatting before insertion

## Notes
- Implemented on 2026-04-15 in `packages/editor/src/editor/extensions/smartPaste.ts` by tracking a short-lived `Cmd/Ctrl+Shift+V` intent window so the next paste prefers `text/plain` even when the clipboard also carries HTML, while preserving the default HTML-to-Markdown smart-paste path and existing image-paste priority.
- Coverage lives in `packages/editor/src/editor/__tests__/smartPaste.test.ts` and currently verifies default rich-text conversion, both `Ctrl+Shift+V` and `Cmd+Shift+V` plain-text shortcut paths, one-shot shortcut reset, blur reset, 1-second timeout expiry, and image-paste precedence.
- Source references used during implementation: https://support.typora.io/Copy-Paste/ and https://support.typora.io/Shortcut-Keys/.
- Ledger truth remains: `status=ready`, `passes=false`, `lastVerifiedAt=null`.

### Latest Verification
- 2026-04-21T16:31:46+08:00 session start:
  - `pnpm harness:start`
  - `./harness/init.sh --smoke`
- 2026-04-21T16:31:46+08:00 focused automation:
  - `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`
  - `pnpm --filter @markflow/editor lint`
  - `pnpm --filter @markflow/editor build`
  - `pnpm harness:verify`
- Results:
  - `pnpm harness:start` passed and selected `MF-076` as the next recommended feature.
  - `./harness/init.sh --smoke` passed on the current tree:
    - `packages/desktop`: `10` test files / `67` tests passed.
    - `packages/editor`: `43` test files / `471` tests passed / `3` skipped.
  - `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` passed (`1` file / `7` tests).
  - `pnpm --filter @markflow/editor lint` passed.
  - `pnpm --filter @markflow/editor build` passed, with the existing Vite chunk-size warning.
  - `pnpm harness:verify` passed (`features: 135 total | verified=79 | ready=40 | planned=15 | blocked=1 | regression=0`; next: `MF-076`).
  - No editor or desktop source changes were made; the existing implementation remains automation-green.

### Manual Verification State
- Required matrix:
  - Paste from `Microsoft Word`, a webpage, and `Visual Studio Code`.
  - For each source, compare `Cmd/Ctrl+Shift+V` against the default `Cmd/Ctrl+V` path.
- Current blocker:
  - `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` returned no results on 2026-04-21T16:31:46+08:00.
  - `osascript -e 'id of app "Microsoft Word"'` failed on 2026-04-21T16:31:46+08:00 with `Can't get application "Microsoft Word". (-1728)`.
  - `find /Applications /System/Applications /Users/pprp/Applications -maxdepth 4 \( -name 'Microsoft Word.app' -o -name 'Word.app' -o -name 'Microsoft Edge.app' -o -name 'Safari.app' -o -name 'Visual Studio Code.app' \)` only found on 2026-04-21T16:31:46+08:00:
    - `/Applications/Microsoft Edge.app`
    - `/Applications/Safari.app`
    - `/Applications/Visual Studio Code.app`
- Existing partial live evidence from 2026-04-19:
  - A real `Microsoft Edge` copy from `/tmp/mf076-web-source.html` exposed `text/plain` plus `text/html`, and native `Cmd+V` into the packaged MarkFlow app inserted `**Bold** [link](https://example.com) and \`code\`` in Source mode.
  - A real `Visual Studio Code` copy from `/tmp/mf076-code-source.md` exposed `text/plain`, `text/html`, `application/vnd.code.copymetadata`, and `vscode-editor-data`, and native `Cmd+V` into the packaged MarkFlow app inserted `**Bold** [link](https://example.com) and \`code\`` in Source mode.
  - The native `Cmd+Shift+V` webpage case still did not produce a trustworthy paste event (`window.__mf076LastPaste` stayed `null`, and `/tmp/mf076-web-plain.md` stayed empty).
- Conclusion:
  - Do not mark `passes=true` or promote the ledger until a trusted desktop session completes the full Word/webpage/VS Code matrix with and without the shortcut.

## Steps
- Copy rich-text content from an external source.
- Press `Cmd/Ctrl+Shift+V` in the editor and confirm the inserted text contains no Markdown conversion and no HTML residue.
- Repeat with `Cmd/Ctrl+V` and confirm smart-paste conversion (MF-026) still applies on the default paste path.

## Automated Verification
- `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`
- `pnpm --filter @markflow/editor lint`
- `pnpm --filter @markflow/editor build`

## Manual Verification
- Paste from `Microsoft Word`, a webpage, and `Visual Studio Code` with and without the shortcut and compare results.
- Only set `passes=true` when the full matrix completes in a trusted desktop session with `Microsoft Word.app` available.
