# MF-076 - Paste as plain text shortcut strips rich formatting before insertion

## Notes
Implemented on 2026-04-15 in `packages/editor/src/editor/extensions/smartPaste.ts` by tracking a short-lived Cmd/Ctrl+Shift+V intent window so the next paste uses `text/plain` even when the clipboard also carries HTML, while preserving the default HTML→Markdown smart-paste path and existing image-paste priority. Focused coverage in `packages/editor/src/editor/__tests__/smartPaste.test.ts` now verifies default rich-text conversion, both `Ctrl+Shift+V` and `Cmd+Shift+V` plain-text shortcut paths, one-shot shortcut reset, blur reset, 1-second timeout expiry, and image-paste precedence. Sources: https://support.typora.io/Copy-Paste/ and https://support.typora.io/Shortcut-Keys/.

Updated on 2026-04-19: re-ran the session-start protocol (`pnpm harness:start`, `./harness/init.sh --smoke`) plus the required automated verification (`pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`) on the current tree, and all of those commands passed again after expanding `smartPaste.test.ts` with the missing macOS shortcut and reset-window coverage. I then built the current-source packaged desktop app with `pnpm desktop:pack`, unpacked it to `/tmp/mf076-markflow-app/MarkFlow.app`, and pushed the manual verification further with a live browser clipboard probe: a real `Microsoft Edge` page copy exposed `«class HTML»` plus plain-text clipboard flavors for `Bold link and code`, and a native `Cmd+V` into the packaged MarkFlow app saved `**Bold** [link](https://example.com/) and \`code\`` to disk, confirming the default webpage paste path still takes the HTML-to-markdown conversion route. The ledger still cannot move to `status=verified` or `passes=true` in this environment, because the full acceptance matrix remains incomplete: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returns no `Microsoft Word.app` bundle, the native `Cmd+Shift+V` desktop automation path did not produce a trustworthy paste event for the webpage case, and the `Code` source probe did not yield stable clipboard evidence. I did not substitute Pages/TextEdit for Word or treat the split automation probes as manual proof, so `harness/feature-ledger.json` remains truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Follow-up on 2026-04-19: re-ran the required automated verification on the current tree (`pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`) and rebuilt the current-source packaged desktop app with `pnpm desktop:pack`, unpacked to `/tmp/mf076-packaged/MarkFlow.app`. Live probes against the packaged app narrowed the blocker further. A real `Microsoft Edge` copy from `/tmp/mf076-web-source.html` reached MarkFlow with `text/plain` and `text/html` clipboard types, and native `Cmd+V` inserted `**Bold** [link](https://example.com) and \`code\`` in Source mode, confirming the default webpage conversion path end-to-end. A real `Visual Studio Code` copy from `/tmp/mf076-code-source.md` reached MarkFlow with `text/plain`, `text/html`, `application/vnd.code.copymetadata`, and `vscode-editor-data`, and native `Cmd+V` inserted `**Bold** [link](https://example.com) and \`code\`` unchanged in Source mode. The ledger still cannot move to `status=verified` or `passes=true` in this environment, because the native `Cmd+Shift+V` automation path into the packaged app still did not fire any paste event for the webpage case (`window.__mf076LastPaste` stayed `null` and `/tmp/mf076-web-plain.md` stayed empty), and `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returns no `Microsoft Word.app` bundle. I kept `harness/feature-ledger.json` truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Follow-up on 2026-04-20: re-ran the session-start protocol (`pnpm harness:start`, `./harness/init.sh --smoke`) and the required automated verification (`pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`) on the current tree, and all of those commands passed again. I did not promote `harness/feature-ledger.json`, because the manual acceptance matrix is still blocked by the environment: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` again returned no `Microsoft Word.app` bundle, and an `/Applications` probe only exposed `Microsoft Edge.app`, `Safari.app`, and `Visual Studio Code.app` among the required external sources. Given that missing Word alone keeps the matrix incomplete, I did not reuse the earlier partial webpage/VS Code probes to overstate completion. `status` remains `ready`, `passes` remains `false`, and `lastVerifiedAt` remains `null` until the Word/webpage/VS Code with-and-without-shortcut checklist is completed in a trusted desktop session.

Second rerun on 2026-04-20: repeated the same one-feature closeout loop on the current tree and kept the ledger unchanged. `pnpm harness:start`, `./harness/init.sh --smoke`, `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` all passed again. I re-checked the manual acceptance gate before touching the ledger: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and an `/Applications` probe only found `Microsoft Edge.app` and `Visual Studio Code.app` among the required external sources. Because the Word/webpage/VS Code matrix still cannot be completed honestly on this machine, I did not rerun partial packaged-app clipboard probes or change `status`, `passes`, or `lastVerifiedAt`.

Third rerun on 2026-04-20: repeated the session-start protocol (`pnpm harness:start`, `./harness/init.sh --smoke`) and the required `MF-076` automated verification (`pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`) on the current tree, and all of those commands passed again. I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual acceptance explicitly depends on Word/webpage/VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept the ledger truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Fourth rerun on 2026-04-20: repeated the same single-feature closeout loop without touching the editor implementation. `pnpm harness:start` still pointed at `MF-076`, `./harness/init.sh --smoke` passed on the current tree, and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. I re-checked the manual acceptance gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I left the ledger truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Fifth rerun on 2026-04-20: repeated the required one-feature closeout loop on the current tree and kept the ledger unchanged. `pnpm harness:start` still surfaced the single-feature workflow, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` files / `65` tests; `packages/editor`: `43` files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. I re-checked the manual acceptance gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I left the ledger truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Sixth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` and `./harness/init.sh --smoke` both passed on the current tree (`packages/desktop`: `10` files / `65` tests; `packages/editor`: `43` files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the manual acceptance gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I left the ledger truthful at `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Seventh rerun on 2026-04-20: repeated the same one-feature closeout loop on the current tree and kept the ledger unchanged. `pnpm harness:start` still pointed at `MF-076`, `./harness/init.sh --smoke` passed again on the current tree (`packages/desktop`: `10` files / `65` tests; `packages/editor`: `43` files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Eighth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` and `./harness/init.sh --smoke` completed at session start, and the required `MF-076` automated verification passed on the current tree: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` still exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Ninth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Tenth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Eleventh rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Twelfth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` again returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Thirteenth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Fourteenth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. I also retried desktop-app automation discovery and still hit the same `Computer Use` Apple event error `-1743`, so there is still no trustworthy native-control fallback for the missing Word/manual-shortcut path in this session. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Fifteenth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree, and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: both `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` and `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' \)` returned no results, `/Applications` still exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, and `Computer Use` app enumeration still failed with Apple event error `-1743`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, and there is still no trustworthy native desktop-control fallback in this session, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Sixteenth rerun on 2026-04-20: repeated the same single-feature closeout loop on the current tree and kept the ledger unchanged again. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the environment gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' \)` still returned no results, and `/Applications` exposed `Microsoft Edge.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`. Because the required manual matrix still depends on Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Seventeenth rerun on 2026-04-20: repeated the startup and closeout loop on the current tree without touching the editor implementation. `./harness/init.sh --smoke` passed on the current tree, and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I also re-ran `pnpm harness:start`; its console still surfaced an old `MF-051` progress excerpt, so feature selection for this session was reconciled against `harness/feature-ledger.json` and `pnpm harness:verify`, which both still identify `MF-076` as the unresolved feature. I re-checked the manual acceptance gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` returned no results, and `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' -o -name 'Microsoft Edge.app' -o -name 'Safari.app' -o -name 'Visual Studio Code.app' \)` only found `/Applications/Microsoft Edge.app`, `/Applications/Safari.app`, and `/Applications/Visual Studio Code.app`. Because the required manual matrix still depends on Microsoft Word plus webpage and VS Code paste comparisons with and without `Cmd/Ctrl+Shift+V`, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Eighteenth rerun on 2026-04-20: repeated the required closeout loop on the current tree without touching the editor implementation. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree, and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the manual acceptance gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` returned no results, `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' -o -name 'Microsoft Edge.app' -o -name 'Safari.app' -o -name 'Visual Studio Code.app' \) 2>/dev/null | sort` only found `/Applications/Microsoft Edge.app`, `/Applications/Safari.app`, and `/Applications/Visual Studio Code.app`, and there is still no trustworthy way to complete the required Word/webpage/VS Code with-and-without-shortcut matrix on this machine. Because the manual acceptance gate is still incomplete, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

Nineteenth rerun on 2026-04-20: repeated the same one-feature closeout loop on the current tree and again left the ledger untouched. `pnpm harness:start` completed at session start, `./harness/init.sh --smoke` passed on the current tree (`packages/desktop`: `10` test files / `65` tests; `packages/editor`: `43` test files / `467` tests / `3` skipped), and the required `MF-076` automated verification passed again: `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts` (`1` file, `7` tests), `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify` (`features: 121 total | verified=75 | ready=30 | planned=15 | blocked=1 | regression=0`; next: `MF-076`). I re-checked the manual acceptance gate before touching `harness/feature-ledger.json`: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` still returned no results, and `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' -o -name 'Microsoft Edge.app' -o -name 'Safari.app' -o -name 'Visual Studio Code.app' \) 2>/dev/null | sort` only found `/Applications/Microsoft Edge.app`, `/Applications/Safari.app`, and `/Applications/Visual Studio Code.app`. Because the required Word/webpage/VS Code with-and-without-shortcut matrix still cannot be completed honestly on this machine, I kept `status=ready`, `passes=false`, and `lastVerifiedAt=null`.

## Steps
- Copy rich-text content from an external source.
- Press Cmd/Ctrl+Shift+V in the editor and confirm the inserted text contains no markdown conversion and no HTML residue.
- Repeat with Cmd/Ctrl+V and confirm smart-paste conversion (MF-026) still applies on the default paste path.

## Automated Verification
- pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/smartPaste.test.ts
- pnpm --filter @markflow/editor lint
- pnpm --filter @markflow/editor build

## Manual Verification
- Paste from three rich sources (Word, webpage, VS Code) with and without the shortcut and compare results.
- 2026-04-19 partial live evidence:
  - A real `Microsoft Edge` page copy from `/tmp/mf076-web-source.html` exposed `text/plain` plus `text/html` clipboard flavors, and native `Cmd+V` into `/tmp/mf076-packaged/MarkFlow.app` inserted `**Bold** [link](https://example.com) and \`code\`` in MarkFlow Source mode.
  - A real `Visual Studio Code` copy from `/tmp/mf076-code-source.md` exposed `text/plain`, `text/html`, `application/vnd.code.copymetadata`, and `vscode-editor-data`, and native `Cmd+V` into `/tmp/mf076-packaged/MarkFlow.app` inserted `**Bold** [link](https://example.com) and \`code\`` in MarkFlow Source mode.
  - The native `Cmd+Shift+V` automation path still did not produce any paste event for the packaged-app webpage case (`window.__mf076LastPaste` stayed `null`, and `/tmp/mf076-web-plain.md` stayed empty), so the plain-text shortcut step remains unverified.
  - `Microsoft Word.app` is still not installed in this environment (`mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` returned no results).
  - Latest 2026-04-20 environment re-check: `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 closeout re-check: `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 tenth rerun re-check: `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 eleventh rerun re-check: `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 twelfth rerun re-check: `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 thirteenth rerun re-check: `/Applications` exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 fifteenth rerun re-check: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` and `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' \)` both returned no results, `/Applications` still exposed `Codex.app`, `Microsoft Edge.app`, `Microsoft Outlook.app`, `Microsoft PowerPoint.app`, `Safari.app`, and `Visual Studio Code.app`, and `Computer Use` app enumeration still failed with Apple event error `-1743`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 sixteenth rerun re-check: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` and `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' \)` both returned no results, and `/Applications` exposed `Microsoft Edge.app`, `Safari.app`, and `Visual Studio Code.app`, but still no `Microsoft Word.app`, so the full three-source matrix remains blocked.
  - Latest 2026-04-20 seventeenth rerun re-check: `mdfind "kMDItemCFBundleIdentifier == 'com.microsoft.Word'"` returned no results, and `find /Applications ~/Applications -maxdepth 3 \( -name 'Microsoft Word.app' -o -name 'Word.app' -o -name 'Microsoft Edge.app' -o -name 'Safari.app' -o -name 'Visual Studio Code.app' \)` only found `/Applications/Microsoft Edge.app`, `/Applications/Safari.app`, and `/Applications/Visual Studio Code.app`, so the full three-source matrix remains blocked.
