# MF-115 - Superscript and subscript inline render unblocks MF-037

## Notes
Tracks the follow-up work to unblock MF-037 without reopening the original entry retroactively. Keeps the blocked entry honest while capturing the plan.

## Steps
- Review the reason MF-037 is currently marked blocked and document the constraint in its notes.
- Add an inline decoration similar to `packages/editor/src/editor/decorations/inlineHtmlDecoration.ts` that renders `~sub~` and `^sup^` (or the agreed Typora syntax) without mutating the source.
- Cover the decoration with focused tests in `packages/editor/src/editor/__tests__/` and confirm it co-exists with existing emphasis parsing.

## Automated Verification
- pnpm --filter @markflow/editor exec vitest run
- pnpm --filter @markflow/editor lint
- pnpm --filter @markflow/editor build

## Manual Verification
- Type sub/sup syntax in WYSIWYG mode and confirm the rendered output matches Typora.
