# MF-115 - Superscript and subscript inline render unblocks MF-037

## Notes
Tracks the follow-up work to unblock MF-037 without reopening the original entry retroactively. Keeps the blocked entry honest while capturing the plan.

Implementation note (2026-04-28): MF-037 was blocked because the existing superscript regex treated adjacent footnote references like `[^1] ... [^2]` as one `^...^` span. This run kept the active scope on MF-115, added focused inline-decoration coverage for `^sup^`, `~sub~`, cursor reveal, emphasis coexistence, and the footnote collision, then guarded single-marker matches so footnote references and strikethrough delimiters are not consumed as superscript/subscript. The source markdown remains unchanged while WYSIWYG decorations hide delimiters away from the caret.

## Steps
- Review the reason MF-037 is currently marked blocked and document the constraint in these notes.
- Add an inline decoration similar to `packages/editor/src/editor/decorations/inlineHtmlDecoration.ts` that renders `~sub~` and `^sup^` without mutating the source.
- Cover the decoration with focused tests in `packages/editor/src/editor/__tests__/` and confirm it co-exists with existing emphasis parsing.

## Automated Verification
- 2026-04-28T11:12:48+0800:
  - `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/inlineDecorations.test.tsx -t "superscript and subscript"` failed before the fix on the footnote collision, then passed after the marker-boundary guard.
  - `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/inlineDecorations.test.tsx` passed (`22` tests).
  - `pnpm --filter @markflow/editor exec vitest run` passed (`47` files, `551` passed / `3` skipped).
  - `pnpm --filter @markflow/editor lint` passed.
  - `pnpm --filter @markflow/editor build` passed with the existing Vite large-chunk warning.

## Manual Verification
- Covered through focused jsdom editor rendering tests in this terminal run: `x^2^` and `H~2~O` render as decorated inline spans away from the caret, raw delimiters reappear when the caret enters the span, adjacent footnotes remain raw, and the document source is unchanged.
