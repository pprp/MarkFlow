# MF-074 - Command palette (Cmd/Ctrl+Shift+P) invokes any editor or desktop action by name

## Notes
Implemented on 2026-04-16 in `packages/editor/src/App.tsx`, `packages/editor/src/components/CommandPalette.tsx`, `packages/editor/src/components/CommandPalette.css`, `packages/editor/src/components/commandPaletteRegistry.ts`, `packages/editor/src/editor/MarkFlowEditor.tsx`, and `packages/editor/src/editor/extensions/smartInput.ts`. This run adds a centered `Cmd/Ctrl+Shift+P` command launcher with prefix/fuzzy ranking, category/shortcut metadata, and a representative cross-surface registry covering view, insert, export, navigation, edit, and file actions; the app now dispatches editor mutations through a small imperative editor bridge so palette-triggered edits can execute against the live CodeMirror selection and refocus the editor after completion. Automated verification passed with `pnpm --filter @markflow/editor exec vitest run src/components/commandPalette.test.tsx src/__tests__/App.test.tsx`, `pnpm --filter @markflow/editor exec eslint src/App.tsx src/editor/MarkFlowEditor.tsx src/editor/extensions/smartInput.ts src/components/CommandPalette.tsx src/components/commandPaletteRegistry.ts src/components/commandPalette.test.tsx`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`; those checks cover palette ranking plus representative editor, desktop-action, and shortcut-regression paths rather than every registered command. `passes` remains false because the required manual five-category command walkthrough has not been completed in this terminal session. Typora's official docs support the surrounding menu/shortcut surface and quick-open behavior, but this run did not find evidence for a first-party Typora command-palette UI, so this entry should be treated as a MarkFlow command launcher rather than a confirmed Typora parity claim. Sources: https://support.typora.io/Shortcut-Keys/ ; https://support.typora.io/Search/ ; https://support.typora.io/Quick-Start/ ; https://support.typora.io/What%27s-New-1.7/.

Updated on 2026-04-19: live verification completed against the current-source packaged desktop app built with `pnpm desktop:pack`, extracted to `/tmp/markflow-mf074-app/MarkFlow.app`, and launched under isolated `HOME=/tmp/mf074-home-*` state so the user profile was not reused. A CDP probe opened `/tmp/mf074-live.md`, loaded `/tmp` as the active vault, and exercised five palette categories without touching menus: `quick op` opened Quick Open and listed `/tmp` entries including `mf074-live.md`; `tog foc` enabled `.mf-focus-mode`; `ins tab` inserted the 3-row table scaffold; `undo` removed the scaffold and restored `Alpha beta gamma paragraph.` as the active line; and `exp h` entered the real HTML export path with `#mf-export-container` mounted and the command palette still engaged until a native `Esc` cancellation unwound the save flow. The same automated verification commands were rerun successfully on 2026-04-19, so the ledger can now truthfully move to `status=verified`, `passes=true`, and `lastVerifiedAt=2026-04-19T12:22:44.211Z`.

## Steps
- Press Cmd/Ctrl+Shift+P and confirm a searchable command overlay opens centered above the editor.
- Filter by prefix and confirm matching commands (toggle WYSIWYG, toggle outline, export HTML, insert table, etc.) appear ranked by relevance.
- Execute a selected command and confirm the editor performs it and the overlay closes without losing selection or scroll position.

## Automated Verification
- Add `commandPalette.test.tsx` asserting registration, fuzzy filtering, and execution dispatch against a representative action registry.

## Manual Verification
- Exercise five categories of commands (view, insert, export, navigation, edit) through the palette without touching menus.
