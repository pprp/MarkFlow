# MF-093 - Source mode displays a configurable line-number gutter

## Notes
Implemented on 2026-04-16 in `packages/editor/src/sourceLineNumbers.ts`, `packages/editor/src/editor/MarkFlowEditor.tsx`, `packages/editor/src/App.tsx`, `packages/editor/src/styles/global.css`, `packages/editor/src/editor/__tests__/MarkFlowEditor.test.tsx`, and `packages/editor/src/__tests__/App.test.tsx`. MarkFlow now enables a 1-based CodeMirror line-number gutter by default in Source mode only, exposes a renderer-local `Source line-number settings` preference that can hide the gutter without recreating the editor, and keeps Preview/WYSIWYG, Reading, and Split views free of the line-number column regardless of the preference state. Automated verification passed with `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/MarkFlowEditor.test.tsx src/__tests__/App.test.tsx`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. `passes` remains false because the required 100+ line gutter-alignment and folded-section manual verification was not truthfully completed in this terminal session. Source: https://support.typora.io/Preferences/.

## Steps
- Toggle source mode and confirm a left-side gutter shows 1-based line numbers aligned with content.
- Expose a preference to hide the gutter and confirm toggling it off removes the column without reflowing decorations.
- Confirm WYSIWYG mode never shows the gutter regardless of setting.

## Automated Verification
- pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/MarkFlowEditor.test.tsx src/__tests__/App.test.tsx

## Manual Verification
- Verify gutter alignment across 100+ line files and after folding sections.
