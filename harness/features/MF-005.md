# MF-005 - List markers and blockquotes render contextually in WYSIWYG mode

## Notes
Covered by `packages/editor/src/editor/__tests__/listAndBlockquoteDecoration.test.tsx`, including unordered lists, ordered lists, task lists, blockquotes, nested lists, nested blockquotes, and direct task-checkbox toggling back into markdown source. Nested blockquote rendering was fixed to avoid duplicate line decorations and to hide stacked `>` prefixes when the caret is outside the quote tree.

Verified 2026-04-23T22:10:15+08:00: Added coverage for distinct nested rendered marker levels and nested task checkbox indentation preservation. Verified with `pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/listAndBlockquoteDecoration.test.tsx`.

## Steps
- Open markdown with unordered lists, ordered lists, task lists, nested lists, and nested blockquotes.
- Move the caret between those blocks and adjacent plain text.
- Confirm syntax markers or checkbox widgets hide when appropriate and remain editable when the caret is inside the block.

## Automated Verification
- pnpm --filter @markflow/editor test:run
- pnpm --filter @markflow/editor exec vitest run src/editor/__tests__/listAndBlockquoteDecoration.test.tsx

## Manual Verification
- Optional visual smoke-check in the app that nested list indentation and blockquote styling match the rendered widgets.
