# MF-107 - Export pipeline renders the active document to PDF and HTML from the desktop menu

## Notes
Implemented on 2026-04-17 in `packages/editor/src/export/htmlExport.ts`, `packages/editor/src/App.tsx`, `packages/desktop/src/main/fileManager.ts`, and the focused desktop/editor test suites. MarkFlow now serializes the hidden WYSIWYG export editor into standalone HTML with inlined stylesheet text, resolved `--mf-*` theme variables, heading-anchor `id`s, normalized internal links, and print CSS that keeps tables/code/math blocks from fragmenting awkwardly; PDF export now renders through a temporary HTML file instead of a giant data URL so long documents remain exportable, waits for fonts/images before printing, enables Electron's experimental PDF outline generation, and surfaces writable-path failures through a desktop error dialog instead of failing silently. Automated verification passed on 2026-04-17 with `pnpm --filter @markflow/desktop exec vitest run`, `pnpm --filter @markflow/editor exec vitest run`, `pnpm --filter @markflow/desktop lint`, `pnpm --filter @markflow/editor lint`, `pnpm --filter @markflow/desktop build`, `pnpm --filter @markflow/editor build`, and `pnpm harness:verify`. `passes` remains false because the required macOS manual export comparison against the live in-app render was not completed in this terminal session.

## Steps
- From File → Export, choose HTML; confirm the exported file matches the rendered WYSIWYG view including code highlighting, math, and tables.
- From File → Export, choose PDF; confirm the exported PDF paginates cleanly on a long document (e.g. `harness/fixtures/mf-large-180k.md`) with working internal heading links.
- Verify the exporter honors the active theme (light vs. dark) and active heading-numbering toggle, and reports errors without crashing when the target path is not writable.

## Automated Verification
- pnpm --filter @markflow/desktop exec vitest run
- pnpm --filter @markflow/editor exec vitest run

## Manual Verification
- Export a representative document to HTML and PDF and compare against the in-app render on macOS.
