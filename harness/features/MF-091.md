# MF-091 - Always-on-top window toggle persists per-window across sessions

## Notes
Implemented with a persisted `WindowStateManager` in `packages/desktop/src/main/windowStateManager.ts`, `View -> Always on Top` checkbox wiring in `packages/desktop/src/main/menu.ts` and `packages/desktop/src/main/index.ts`, and shared window-state typing updates in `packages/shared/src/index.ts` plus `packages/editor/src/App.tsx`. Automated coverage in `packages/desktop/src/main/windowStateManager.test.ts` and `packages/desktop/src/main/menu.test.ts` passed on 2026-04-16, alongside `pnpm --filter @markflow/editor exec vitest run src/__tests__/App.test.tsx`, `pnpm lint`, `pnpm test`, and `pnpm build`. `passes` remains false until manual verification confirms restart persistence and platform behavior across macOS Spaces and Windows multi-monitor setups; the current desktop shell still centers on a single editor window, so true multi-window behavior was only covered at the per-window state-manager level in automation. Source: https://support.typora.io/Menu/.

## Steps
- Toggle Always on Top from the View menu and confirm the editor window stays above other apps.
- Close and relaunch the app; confirm the previous window reopens with the same pinned state.
- Confirm the setting is per-window when multiple windows are open.

## Automated Verification
- Add a desktop test stubbing `setAlwaysOnTop` and verifying state persistence read/write.

## Manual Verification
- Confirm behavior across macOS Spaces and Windows multi-monitor setups.
