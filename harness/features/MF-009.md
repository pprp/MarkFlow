# MF-009 - New file flow clears document state, dirty state, and represented filename cleanly

## Notes
Renderer now resets to an empty untitled document on `new-file`, and the Electron main process clears the represented filename; covered by `packages/editor/src/__tests__/App.test.tsx`.

## Steps
- Open an existing file in the desktop app.
- Trigger File > New.
- Confirm the editor resets intentionally, window title changes to `Untitled`, and no stale content remains.

## Automated Verification
- pnpm --filter @markflow/editor test:run

## Manual Verification
- Confirm creating a new file does not overwrite the previously opened file path.
