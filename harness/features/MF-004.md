# MF-004 - Switching between WYSIWYG and source modes preserves document text, selection, and undo history

## Notes
Fixed by preserving a single EditorView and reconfiguring WYSIWYG extensions through a CodeMirror compartment; covered by `packages/editor/src/editor/__tests__/MarkFlowEditor.test.tsx`.

## Steps
- Type into the editor and create unsaved changes.
- Toggle from WYSIWYG to source and back.
- Confirm document content, selection, and history still reflect the live editing session.

## Automated Verification
- pnpm --filter @markflow/editor test:run

## Manual Verification
- Type into the starter document, toggle modes repeatedly, and confirm no edits disappear.
